Create a task list so that travel-planning-cot can track its progress and work in parallel if needed. Allow the work to be started, stopped, and restarted if needed. @prompts/Travel-Prompt-cot.md is a file with travel prompts that are CoT type prompts. The first part of the prompt asks for a profile. Either the AI can ask questions and get answers, or a profile can be in a file. We will use a file @[trips/[destination-year]/[profile-destination-year].md. Each prompt is called and generates a file with the desired response. At the end of all prompts, the newly created file links are compiled into a list of single links within the document. Use the obsidian format. 