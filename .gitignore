# Secrets and local env
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Dependencies
node_modules/

# Pipeline build outputs and local cache (source under pipeline/ is tracked)
pipeline/data/
pipeline/dist/
pipeline/node_modules/
pipeline/.playwright-browsers/

# Cursor MCP — commit example only; keep live config local (contains SERPAPI_API_KEY)
!.cursor/
.cursor/*
!.cursor/mcp.json.example

# OS / editor
.DS_Store
.idea/
.vscode/
