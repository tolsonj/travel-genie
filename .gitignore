# Secrets and local env
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Dependencies
node_modules/

# Pipeline build outputs and local tooling
pipeline/

# Trip planning content (local)
trips/

# Cursor MCP — commit example only; keep live config local (contains SERPAPI_API_KEY)
!.cursor/
.cursor/*
!.cursor/mcp.json.example

# OS / editor
.DS_Store
.idea/
.vscode/
